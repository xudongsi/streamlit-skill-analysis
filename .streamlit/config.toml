# 在 app.py 中直接设置主题
st.set_page_config(
    page_title="技能分析",
    layout="wide",
    initial_sidebar_state="expanded"
)

# 使用内置主题
st.markdown("""
<style>
/* 简单样式 */
body {
    background-color: #0f172a;
    color: white;
}
</style>
""", unsafe_allow_html=True)
